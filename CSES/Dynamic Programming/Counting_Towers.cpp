#include <bits/stdc++.h>
using namespace std;

const int mod=1e9+7;
int dp[1000000];

int main(){
    int t,n;
    cin >> t;
    while (t--)
    {
        cin >> n;
        if(dp[n]==0)
    }
    return 0;
}